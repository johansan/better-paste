# Obsidian resolves releases by exact tag match against manifest.json, so the tag
# must be the bare version. Without this, npm version would tag v1.0.1, which does
# not match the tag filter in .github/workflows/release.yml and would silently
# publish nothing.
tag-version-prefix=""
